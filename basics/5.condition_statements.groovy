def num = 10

if(num==10){
    println("Number is 10")
}
else{
    println("Number is NOT 10")
}

// Check for positive number - Oberve no curly braces
// sigle line is not reequired cutluy lines
def numx = 0
if(numx > 0)
    println("Number is positive")
else if(numx == 0)
    println("Number is zero")
else
    println("Number is negative")
